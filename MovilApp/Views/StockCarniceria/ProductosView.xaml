<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:MovilApp.ViewModels.Productos"
             x:Class="MovilApp.Views.StockCarniceria.ProductosView"
             Title="ProductosView">
    <ContentPage.BindingContext>
        <vm:ProductosViewModel/>
    </ContentPage.BindingContext>
    <Grid RowDefinitions="Auto, *">
        <!-- Botón de regreso -->
        <Button Grid.Row="0"
                Text="←"
                Command="{Binding VolverCommand}"
                BackgroundColor="#f0f0f0"
                TextColor="Black"
                FontSize="24"
                HorizontalOptions="Start"
                VerticalOptions="Center"
                WidthRequest="50"
                HeightRequest="50"
                Margin="0,0,0,20"
                Padding="0,0,0,10"/>
        
        <ScrollView Grid.Row="1" Padding="20">
            <StackLayout Spacing="20">

                <!-- Encabezado -->
                <Frame BackgroundColor="#8B0000" 
                    Padding="20" 
                    CornerRadius="15" 
                    HasShadow="True">
                    <StackLayout>
                        <Label Text="🥩 Buscar Producto" 
                            FontSize="26" 
                            FontAttributes="Bold"
                            TextColor="White"
                            HorizontalOptions="Center"/>
                        <Label Text="Encuentra el producto que buscas" 
                            FontSize="14"
                            TextColor="#FFE4E1"
                            HorizontalOptions="Center"/>
                    </StackLayout>
                </Frame>

                <!-- Sección de búsqueda -->
                <Frame BackgroundColor="White" 
                    Padding="15" 
                    CornerRadius="12" 
                    HasShadow="True"
                    BorderColor="#D2691E">
                    <StackLayout Spacing="15">
                        <Label Text="¿Qué producto estas buscando?" 
                            FontSize="16"
                            FontAttributes="Bold"
                            TextColor="#3E2723"/>

                        <!-- SearchBar mejorado -->
                        <Frame BackgroundColor="#FFF8F0" 
                            Padding="5" 
                            CornerRadius="10"
                            HasShadow="False"
                            BorderColor="#D2691E">
                            <SearchBar Placeholder="Buscar por título, producto.." 
                                    Text="{Binding TextoBusqueda}"
                                    SearchCommand="{Binding BuscarCommand}"
                                    BackgroundColor="Transparent"
                                    PlaceholderColor="#8B7355"
                                    TextColor="#3E2723"
                                    FontSize="16"/>
                        </Frame>
                        <!-- Botón para mostrar/ocultar filtros -->
                        <Button Text="🔍 Buscar" 
                         Command="{Binding BuscarCommand}"
                         BackgroundColor="#A0522D"
                         TextColor="White"
                         CornerRadius="8"
                         FontSize="14"/>


                        <!-- Indicador de carga -->
                        <StackLayout IsVisible="{Binding EstaDescargando}" 
                                 Orientation="Horizontal" 
                                 HorizontalOptions="Center"
                                 Spacing="10">
                            <ActivityIndicator IsRunning="{Binding EstaDescargando}"
                                           Color="#8B0000"/>
                            <Label Text="Buscando productos..." 
                                TextColor="#8B0000"
                                VerticalOptions="Center"
                                FontSize="14"/>
                        </StackLayout>
                    </StackLayout>
                </Frame>

                <!-- Sección de resultados -->
                <StackLayout Spacing="15">
                    <!-- Encabezado de resultados -->
                    <Frame BackgroundColor="#DEB887" 
                        Padding="12" 
                        CornerRadius="10" 
                        HasShadow="True"
                        BorderColor="#8B4513">
                        <StackLayout>
                            <Label Text=" Resultados de búsqueda" 
                                FontSize="18" 
                                FontAttributes="Bold"
                                TextColor="#3E2723"
                                HorizontalOptions="Center"/>
                            <Label Text="{Binding Productos.Count, StringFormat='{0} productos encontrados'}"
                                FontSize="14"
                                TextColor="#3E2723"
                                HorizontalOptions="Center"/>
                        </StackLayout>
                    </Frame>

                    <!-- Lista de productos en cards mejoradas -->
                    <CollectionView ItemsSource="{Binding Productos}">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Frame BackgroundColor="White" 
                                    Padding="20" 
                                    Margin="0,8"
                                    CornerRadius="15"
                                    HasShadow="True"
                                    BorderColor="#D2691E">
                                    <Grid ColumnDefinitions="Auto, *, Auto" 
                                       RowDefinitions="Auto, Auto, Auto, Auto">

                                        <!-- Ícono principal -->
                                        <Label Grid.Column="0" Grid.Row="0" Grid.RowSpan="4"
                                            Text="🥩" 
                                            FontSize="40"
                                            VerticalOptions="Center"
                                            HorizontalOptions="Center"
                                            Margin="0,0,15,0"/>

                                        <!-- Título del producto -->
                                        <Label Grid.Column="1" Grid.Row="0"
                                            Text="{Binding Nombre}" 
                                            FontSize="18"
                                            FontAttributes="Bold"
                                            TextColor="#3E2723"
                                            LineBreakMode="WordWrap"/>

                                        <!-- Precio -->
                                        <StackLayout Grid.Column="1" Grid.Row="1" 
                                                 Orientation="Horizontal" 
                                                 Spacing="5"
                                                 Margin="0,5,0,0">
                                            <Label Text="💲" FontSize="14"/>
                                            <Label Text="{Binding PrecioUnitario, StringFormat='${0:N2}'}" 
                                                FontSize="16"
                                                TextColor="#8B0000"
                                                FontAttributes="Bold"
                                                LineBreakMode="NoWrap"/>
                                        </StackLayout>

                                        <!-- Categoría -->
                                        <StackLayout Grid.Column="1" Grid.Row="2" 
                                                 Orientation="Horizontal" 
                                                 Spacing="5"
                                                 Margin="0,2,0,0">
                                            <Label Text="🏷️" FontSize="14"/>
                                            <Label Text="{Binding Categoria}" 
                                                FontSize="14"
                                                TextColor="#8B7355"/>
                                        </StackLayout>


                                        <!-- Botón de acción -->
                                        <Button Grid.Column="2" Grid.Row="0" Grid.RowSpan="4"
                                             Text="📋&#x0a;Ver"
                                             BackgroundColor="#A0522D"
                                             TextColor="White"
                                             CornerRadius="10"
                                             FontSize="12"
                                             WidthRequest="60"
                                             HeightRequest="60"
                                             VerticalOptions="Center"/>


                                    </Grid>
                                </Frame>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </Grid>
</ContentPage>